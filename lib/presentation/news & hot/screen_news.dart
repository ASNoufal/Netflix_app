import 'package:flutter/material.dart';

class NewsandHot extends StatelessWidget {
  const NewsandHot({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("News and hot"),
    );
  }
}
